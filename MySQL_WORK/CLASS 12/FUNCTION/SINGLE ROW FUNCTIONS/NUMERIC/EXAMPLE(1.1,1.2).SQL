SHOW DATABASES;

USE CARSHOWROOM;

#EXAMPLE 1.1 FROM CBSE CLASS 12 TEXTBOOK
#A)
SELECT ROUND(12/100* PRICE, 1) "GST" FROM INVENTORY;

#B)
ALTER TABLE INVENTORY DROP FINALPRICE;
ALTER TABLE INVENTORY ADD FINALPRICE DECIMAL(10,2) NOT NULL;
UPDATE INVENTORY SET FINALPRICE = PRICE + ROUND(0.12* PRICE, 1);
SELECT * FROM INVENTORY;

#C)
SELECT CARID, FINALPRICE,
ROUND((FINALPRICE - MOD(FINALPRICE, 10000))/10, 0) "EMI" FROM INVENTORY;

#D)

SELECT CARID, FINALPRICE,
ROUND((FINALPRICE - MOD(FINALPRICE, 10000))/10, 0) "EMI",
MOD(FINALPRICE, 10000)"REMAINING AMOUNT" FROM INVENTORY;

#EXAMPLE 1.2 FROM CBSE CLASS 12 TEXTBOOK

#A) 

ALTER TABLE SALES DROP COMMISSION;
ALTER TABLE SALES ADD COMMISSION DECIMAL(7,2) NOT NULL;
DESC SALES;

#B)

UPDATE SALES SET COMMISSION = 0.12 * SALESPRICE;
SELECT * FROM SALES WHERE COMMISSION > 73000;

#C)

SELECT INVOICENO, SALESPRICE, COMMISSION, ROUND(COMMISSION, 0) "ROUND OFF COMMISSION" FROM SALES;

